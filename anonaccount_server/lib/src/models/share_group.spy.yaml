class: ShareGroup
table: share_group
fields:
  id: UuidValue?, defaultPersist=random
  ultimateSigningPublicKeyHex: String
  ultimatePublicKey: String
  encryptedDataKey: String
  createdAt: DateTime, default=now
  keyEpoch: int, default=0
