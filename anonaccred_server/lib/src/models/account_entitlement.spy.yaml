class: AccountEntitlement
table: account_entitlement
fields:
  accountId: int, relation(parent=anon_account, onDelete=Cascade)
  entitlement: Entitlement?, relation
  balance: double # Current running total
indexes:
  account_entitlement_idx:
    fields: accountId, entitlementId
    unique: true
