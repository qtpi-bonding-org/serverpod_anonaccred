class: EphemeralAccreditation
table: ephemeral_accreditation
fields:
  accountUuid: UuidValue
  transactionTimestamp: DateTime # MATCH KEY (Exact Store Time)
indexes:
  lookup_idx:
    fields: transactionTimestamp
    unique: false
